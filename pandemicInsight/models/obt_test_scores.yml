version: 2

models:
  - name: obt_test_scores
    description: The order fulfillment fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per year per state per grade per subject